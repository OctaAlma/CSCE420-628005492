# (or WAR WAG WAB )

WAR WAG WAB

# (or NTR NTG NTB )

NTR NTG NTB

# (or SAR SAG SAB )

SAR SAG SAB

# (or QR QG QB )

QR QG QB

# (or NSWR NSWG NSWB )

NSWR NSWG NSWB

# (or VR VG VB )

VR VG VB

# (or TR TG TB )

TR TG TB

# (implies WAR (not NTR))

-WAR -NTR

# (implies WAR (not SAR))

-WAR -SAR

# (implies WAG (not NTG))

-WAG -NTG

# (implies WAG (not SAG))

-WAG -SAG

# (implies WAB (not NTB))

-WAB -NTB

# (implies WAB (not SAB))

-WAB -SAB

# (implies NTR (not WAR))

-NTR -WAR

# (implies NTR (not SAR))

-NTR -SAR

# (implies NTR (not QR))

-NTR -QR

# (implies NTG (not WAG))

-NTG -WAG

# (implies NTG (not SAG))

-NTG -SAG

# (implies NTG (not QG))

-NTG -QG

# (implies NTB (not WAB))

-NTB -WAB

# (implies NTB (not SAB))

-NTB -SAB

# (implies NTB (not QB))

-NTB -QB

# (implies SAR (not WAR))

-SAR -WAR

# (implies SAR (not NTR))

-SAR -NTR

# (implies SAR (not QR))

-SAR -QR

# (implies SAR (not NSWR))

-SAR -NSWR

# (implies SAR (not VR))

-SAR -VR

# (implies SAG (not WAG))

-SAG -WAG

# (implies SAG (not NTG))

-SAG -NTG

# (implies SAG (not QG))

-SAG -QG

# (implies SAG (not NSWG))

-SAG -NSWG

# (implies SAG (not VG))

-SAG -VG

# (implies SAB (not WAB))

-SAB -WAB

# (implies SAB (not NTB))

-SAB -NTB

# (implies SAB (not QB))

-SAB -QB

# (implies SAB (not NSWB))

-SAB -NSWB

# (implies SAB (not VB))

-SAB -VB

# (implies QR (not NTR))

-QR -NTR

# (implies QR (not SAR))

-QR -SAR

# (implies QR (not NSWR))

-QR -NSWR

# (implies QG (not NTG))

-QG -NTG

# (implies QG (not SAG))

-QG -SAG

# (implies QG (not NSWG))

-QG -NSWG

# (implies QB (not NTB))

-QB -NTB

# (implies QB (not SAB))

-QB -SAB

# (implies QB (not NSWB))

-QB -NSWB

# (implies NSWR (not QR))

-NSWR -QR

# (implies NSWR (not SAR))

-NSWR -SAR

# (implies NSWR (not VR))

-NSWR -VR

# (implies NSWG (not QG))

-NSWG -QG

# (implies NSWG (not SAG))

-NSWG -SAG

# (implies NSWG (not VG))

-NSWG -VG

# (implies NSWB (not QB))

-NSWB -QB

# (implies NSWB (not SAB))

-NSWB -SAB

# (implies NSWB (not VB))

-NSWB -VB

# (implies VR (not SAR))

-VR -SAR

# (implies VR (not NSWR))

-VR -NSWR

# (implies VG (not SAG))

-VG -SAG

# (implies VG (not NSWG))

-VG -NSWG

# (implies VB (not SAB))

-VB -SAB

# (implies VB (not NSWB))

-VB -NSWB

# (<-> WAR (and (not WAG) (not WAB)))

-WAG -WAR
-WAB -WAR
WAG WAB WAR

# (<-> WAG (and (not WAR) (not WAB)))

-WAR -WAG
-WAB -WAG
WAR WAB WAG

# (<-> WAB (and (not WAR) (not WAG)))

-WAR -WAB
-WAG -WAB
WAR WAG WAB

# (<-> NTR (and (not NTG) (not NTB)))

-NTG -NTR
-NTB -NTR
NTG NTB NTR

# (<-> NTG (and (not NTR) (not NTB)))

-NTR -NTG
-NTB -NTG
NTR NTB NTG

# (<-> NTB (and (not NTR) (not NTG)))

-NTR -NTB
-NTG -NTB
NTR NTG NTB

# (<-> SAR (and (not SAG) (not SAB)))

-SAG -SAR
-SAB -SAR
SAG SAB SAR

# (<-> SAG (and (not SAR) (not SAB)))

-SAR -SAG
-SAB -SAG
SAR SAB SAG

# (<-> SAB (and (not SAR) (not SAG)))

-SAR -SAB
-SAG -SAB
SAR SAG SAB

# (<-> QR (and (not QG) (not QB)))

-QG -QR
-QB -QR
QG QB QR

# (<-> QG (and (not QR) (not QB)))

-QR -QG
-QB -QG
QR QB QG

# (<-> QB (and (not QR) (not QG)))

-QR -QB
-QG -QB
QR QG QB

# (<-> NSWR (and (not NSWG) (not NSWB)))

-NSWG -NSWR
-NSWB -NSWR
NSWG NSWB NSWR

# (<-> NSWG (and (not NSWR) (not NSWB)))

-NSWR -NSWG
-NSWB -NSWG
NSWR NSWB NSWG

# (<-> NSWB (and (not NSWR) (not NSWG)))

-NSWR -NSWB
-NSWG -NSWB
NSWR NSWG NSWB

# (<-> VR (and (not VG) (not VB)))

-VG -VR
-VB -VR
VG VB VR

# (<-> VG (and (not VR) (not VB)))

-VR -VG
-VB -VG
VR VB VG

# (<-> VB (and (not VR) (not VG)))

-VR -VB
-VG -VB
VR VG VB

# (<-> TR (and (not TG) (not TB)))

-TG -TR
-TB -TR
TG TB TR

# (<-> TG (and (not TR) (not TB)))

-TR -TG
-TB -TG
TR TB TG

# (<-> TB (and (not TR) (not TG)))

-TR -TB
-TG -TB
TR TG TB

